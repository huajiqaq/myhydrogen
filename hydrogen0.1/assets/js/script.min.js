document.querySelectorAll(".datepicker").forEach((function (e) { new Pikaday({ field: e }) }));

function loading() {
    alert("加载下载链接中 请耐心等待")
}

// 创建 XHR 对象
const xhr = new XMLHttpRequest();

// 设置请求方法和 URL
xhr.open('GET', 'https://huajicloud-data.onrender.com/hydrogen.html');

// 发送请求
xhr.send();

// 请求加载完成,处理响应
xhr.onload = function () {
    if (xhr.status === 200) {
        // 解析响应内容
        var downurl = xhr.responseText.match(/updateurl=(\S*),updateurl/)[1];
        document.querySelectorAll(".btn")[0].href = downurl
    }
}

// 请求错误处理
xhr.onerror = function () {
    console.log('Request Error...');
}